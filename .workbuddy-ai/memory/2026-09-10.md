# 2026-09-10

## Library encapsulation pass — completed end to end

Goal: move demo-local boilerplate into the library so examples get shorter.

### New public APIs added

| API | File | Notes |
|---|---|---|
| `sat_ascii_font_init_scaled_indexed8()` | `include/saturn/font.h`, `src/core/font_api.cpp` | Scaled (2x/3x/4x) 8x8 ASCII font init. Was hand-rolled in the demo. |
| `sat_ascii_font_measure_text_scaled_indexed8()` | `src/core/font_logic.hpp` | Width measurement accounting for scale. |
| `sat_vdp2_set_backdrop_color()` | `include/saturn/vdp2.h` | Wraps existing HAL `set_backdrop_color()` (BKTAU/BKTAL + VRAM word). Demo wrote raw VDP2 regs before. |
| `sat_vdp2_rbg0_mode7_init()` + `sat_vdp2_rbg0_mode7_config_t` | `include/saturn/vdp2.h`, `src/core/vdp2_api.cpp` | **Biggest win.** Replaces ~45 lines of hand-written VDP2 register pokes (RAMCTL, CYCA*, CYCB*, CHCTLB, MPOFR, RPTA, RPMD, RPRCTL, KTCTL, KTAOF, PRIR, PRISA, BMPNB, PLSZ, BGON, TVMD, backdrop). |
| `sat_pad_format_held()` / `sat_pad_format_frame()` | `include/saturn/input.h`, `src/core/input_api.cpp` | HUD text formatting helpers (`PAD:xxxx UDLRSABC`, `FRM:xxxxxxxx`). |
| `sat_draw_polygon()` / `sat_draw_polyline()` / `sat_draw_line()` + `sat_polygon_cmd_t` / `sat_line_cmd_t` | `include/saturn/vdp1.h`, `src/core/vdp1_api.cpp`, `src/hal/vdp1.cpp` | New VDP1 non-texture drawing. Command selects: polygon=0x0004, polyline=0x0005, line=0x0006. |

### Supporting refactors

- `src/hal/vdp2.hpp/.cpp`: added `set_rbg0_priority()`, `set_rbg0_sprite_priority()`, `set_rbg0_bitmap_mode_extra()` (KTAOF) setters so `mode7_init` has no raw register access.
- `src/core/logic.hpp`: added pure helpers `compose_polygon_ctrl()` / `compose_polygon_pmod()` + `kVdp1Cmd*` constants. HAL now uses these instead of magic numbers (`0x0004`, `0x0018`, etc. all removed).
- `examples/vdp2_rbg0_ground/main.c`: **-150 lines**. `init_rbg0_mode7()` is now a 10-line struct literal + one call.

### Verification (all green)

- Host tests: **62 pass** (was 57) — added 5 polygon encoding tests to `tests/host/test_vdp1_logic.cpp`.
- Harness probe on final build: VDP2 registers byte-identical to the pre-refactor hand-written values (`ramctl=0x1107`, `bgon=0x1010`, `chctlb=0x1200`, `ktctl=1`, `prir=6`, `ktctl`/`ktaof`/`plsz`/`mpofr`/`rpmd` as expected). 11 RBG0 harness tests pass.
- All buildable examples rebuilt OK.

### Build/toolchain gotchas (recurring)

1. Target build needs MSYS2 SH-2 toolchain on PATH:
   `export PATH="/c/msys64/home/celso/saturn-tools/bin:$PATH"` then `make EXAMPLE=<name> IP_PROFILE=current IP_TEMPLATE_KIND=yaul all`
2. **Host `make test` additionally needs the host g++**: it lives at `/c/msys64/ucrt64/bin`, NOT `mingw64`/`usr`. Without it: `make: g++: No such file or directory` (Error 127).
   `export PATH="/c/msys64/ucrt64/bin:/c/msys64/home/celso/saturn-tools/bin:$PATH"`
3. `examples/input_move` is NOT standalone-buildable — it only contains `Makefile.inc` (shared include consumed by other examples). `make EXAMPLE=input_move` fails with `mkdir: falta operando`. Expected, not a regression.
4. PowerShell: `PATHEXT` corruption still needs the manual fix before invoking `run-harness.ps1`/`build-example.ps1`.
